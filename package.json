{
  "name": "steamcommunity-monster",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix worker",
    "dev": "npm run dev:worker",
    "dev:worker": "npm --prefix worker run dev",
    "test": "node --test tests/worker.test.js",
    "lint": "node --check worker/src/index.js && node --check pages/app.js && node --check pages/config.js && node --check scripts/bootstrap-cloudflare.js && node --check scripts/bootstrap-github.js && node --check scripts/check-d1-binding.js && node --check scripts/ensure-pages-project.js",
    "check": "npm test && npm run lint && npm --prefix worker run deploy:dry-run",
    "deploy:worker": "npm --prefix worker run deploy",
    "deploy:pages": "npx --yes wrangler pages deploy pages --project-name steamcommunity-monster",
    "migrate:remote": "npm --prefix worker run migrate:remote",
    "bootstrap:cloudflare": "node scripts/bootstrap-cloudflare.js",
    "bootstrap:github": "node scripts/bootstrap-github.js"
  }
}
